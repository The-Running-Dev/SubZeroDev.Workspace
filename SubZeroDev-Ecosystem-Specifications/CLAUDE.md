See `AGENTS.md`, and the `AGENTS.md` inside whichever `SubZeroDev.*` directory you are changing.

The instructions live there rather than here so that every agent reads the same file. A second copy
of the guidance would drift from the first, which is the failure this specification set exists to
avoid.
